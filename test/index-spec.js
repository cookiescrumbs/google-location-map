describe('Google location map', function() {

	it('should have a google map', () => {
		expect(false).toEqual(false);
	});

	it('should have a google map', () => {
		expect(false).toEqual(false);
	});

});